# v13.2 build fix

- Renamed shared design symbols `AppColors` -> `IptvColors`.
- Renamed `AppTypography` -> `IptvTypography`.
- Updated all Kotlin references and imports.
- Prevents collisions with stale/legacy `Theme.kt` files from earlier project extractions.
